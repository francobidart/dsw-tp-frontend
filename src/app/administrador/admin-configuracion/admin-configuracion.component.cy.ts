import { AdminConfiguracionComponent } from './admin-configuracion.component'

describe('AdminConfiguracionComponent', () => {
  it('should mount', () => {
    cy.mount(AdminConfiguracionComponent)
  })
})